{
  "name": "liberty-bourse-simulateur",
  "description": "Simulateur de portefeuille boursier Streamlit",
  "repository": "",
  "logo": "",
  "keywords": ["python", "streamlit", "finance", "portfolio"],
  "env": {
    "PORTFOLIO_AUTH_MODE": {
      "description": "Mode auth: off | optional | required",
      "value": "optional"
    },
    "OPENAI_API_KEY": {
      "description": "Optionnel: active l'assistant IA OpenAI",
      "required": false
    },
    "POLYGON_API_KEY": {
      "description": "Optionnel: cotations Polygon",
      "required": false
    }
  },
  "formation": {
    "web": {
      "quantity": 1,
      "size": "basic"
    }
  }
}
